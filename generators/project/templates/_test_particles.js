var CondensationTests = require("condensation-particle-tests");

describe("particles", function() {

  var cTests = new CondensationTests();

  /* EXAMPLE

    it("creates the condition", function() {
      cTests.testParticle(
        "parameter",
        "password",
        require('./fixtures/parameter_password_output_1'),
        {logicalId: "Password", hArgs: {noEcho: true}}
      );
    });

   */

});
